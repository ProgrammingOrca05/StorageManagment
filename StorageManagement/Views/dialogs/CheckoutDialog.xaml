<Window x:Class="StorageManagement.Views.dialogs.CheckoutDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Checkout Confirmation"
        Height="480" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#EAF4FB"
        FontFamily="Segoe UI"
        Icon="/Assets/icons/paper.ico">

    <Window.Resources>

        <!-- ═══ Shared dialog button style ═══ -->
        <Style x:Key="DialogButton" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Width" Value="140"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ══════════════ Header with transaction date ══════════════ -->
        <Border Grid.Row="0"
                Background="#2B8BEB"
                CornerRadius="10"
                Padding="14 10"
                Margin="0 0 0 12">
            <StackPanel>
                <TextBlock Text="Checkout Confirmation"
                           FontSize="18"
                           FontWeight="Bold"
                           Foreground="White">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="0" ShadowDepth="1" Direction="315"/>
                    </TextBlock.Effect>
                </TextBlock>
                <!-- Binding Date from the Transaction DataContext -->
                <TextBlock FontSize="12" Foreground="#D0EAFF" Margin="0 3 0 0">
                    <Run Text="Date: "/>
                    <Run Text="{Binding Date, StringFormat={}{0:dd MMM yyyy  HH:mm}}"/>
                </TextBlock>
            </StackPanel>
        </Border>

        <!-- ══════════════ Order summary label ══════════════ -->
        <TextBlock Grid.Row="1"
                   Text="Order Summary"
                   FontSize="13"
                   FontWeight="SemiBold"
                   Foreground="#1A3A4A"
                   Margin="0 0 0 6"/>

        <!-- ══════════════ Items list bound to Transaction.Items ══════════════ -->
        <ListView Grid.Row="2"
                  ItemsSource="{Binding Items}"
                  BorderBrush="#85C7E8"
                  BorderThickness="1"
                  Margin="0 0 0 12">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Product"    DisplayMemberBinding="{Binding Product.Name}"  Width="160"/>
                    <GridViewColumn Header="Qty"        DisplayMemberBinding="{Binding Quantity}"      Width="50"/>
                    <GridViewColumn Header="Unit Price" DisplayMemberBinding="{Binding Product.Price,  StringFormat={}{0:F2} EGP}" Width="100"/>
                    <GridViewColumn Header="Total"      DisplayMemberBinding="{Binding TotalPrice,     StringFormat={}{0:F2} EGP}" Width="100"/>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- ══════════════ Totals: Subtotal, VAT, Total ══════════════ -->
        <Border Grid.Row="3"
                Background="White"
                CornerRadius="8"
                Padding="14 10"
                Margin="0 0 0 14"
                BorderBrush="#85C7E8"
                BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="6"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Subtotal"   Foreground="#1A3A4A" FontSize="13" Margin="0 2"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Subtotal, StringFormat={}{0:F2} EGP}" Foreground="#1A3A4A" FontSize="13"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="VAT (14%)" Foreground="#1A3A4A" FontSize="13" Margin="0 2"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding VAT,      StringFormat={}{0:F2} EGP}" Foreground="#1A3A4A" FontSize="13"/>

                <Rectangle Grid.Row="2" Grid.ColumnSpan="2" Fill="#85C7E8" Height="1"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Total" FontSize="15" FontWeight="Bold" Foreground="#2B8BEB" Margin="0 4 0 0"/>
                <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Total, StringFormat={}{0:F2} EGP}" FontSize="15" FontWeight="Bold" Foreground="#2B8BEB" Margin="0 4 0 0"/>
            </Grid>
        </Border>

        <!-- ══════════════ Confirm and Cancel buttons ══════════════ -->
        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center">
            <Button Content="✔  Confirm"
                    Style="{StaticResource DialogButton}"
                    Background="#2B8BEB"
                    Margin="0 0 12 0"
                    Click="Confirm_Click"/>
            <Button Content="✖  Cancel"
                    Style="{StaticResource DialogButton}"
                    Background="#4A90D9"
                    Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
